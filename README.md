# blender-analyze-
危机----节点组内部和外部的命名重复，所以不能用命名，，，回退到最开始的，，需要id的link，所以可以改的只有node的ui信息了
当前1和 2版脚本里，“遍历到节点组（Group Node）”的处理逻辑其实很简单：它不会进入节点组内部，只做“当前 node_tree 的一层平面导出”
所以可以应该可以优化，但是目前保持这样也问题不大，其实复杂一点的节点我自己去看也好一点。
那这样说来，我既然不打算进入节点组，那实际上我又真的不需要id 了。
